# PWX SimHub bindings

This directory holds the current SimHub bindings used by the PWX software and hardware.

The purpose of these bindings is to translate raw telemetry and computed sim variables from Simhub and iRacing into a predictable, sanitised API.

### PWX-Core
The core engine that powers all compatible PWX Dashboards and hardware